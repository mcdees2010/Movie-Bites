<div class="addmovie"
<br />
<h6>
<div class="linkcolor"><%= link_to "add a movie", new_movie_path(@movie) %></div>
</h6>
</div>

<div class="allmovies">
    <ul>
        <% @movies.each do |m| %>
            <li><%= link_to image_tag(m.image, width: "250px", height: "360", :style => "padding: 20px;"), movie_path(m) %></li>
        <% end %>
    </ul>
</div>
