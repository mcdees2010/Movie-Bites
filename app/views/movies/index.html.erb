<h1>MOVIES</h1>

<div>
    <ul>
        <% @movies.each do |m| %>
            <li><%= link_to m.title, movie_path(m) %></li>
            <%= image_tag(m.image, width: "250px") %>
        <% end %>
    </ul>
</div>